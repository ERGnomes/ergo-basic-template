# CRA 5 (react-scripts) optional-peers TypeScript ^4 vs Dynamic/viem stack on TS 5 — npm 7+ strict resolution fails without this.
# Matches Railway/Nixpacks: `npm install --legacy-peer-deps`.
legacy-peer-deps=true
